<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/text_input_layout_color</item>
    </style>


    <style name="BtnTheme">
        <item name="android:background">@color/btn_color</item>
    </style>

    <style name="TextInputLayoutTheme" parent="@android:style/TextAppearance">
        <item name="android:textColorHint">@android:color/darker_gray</item>
        <item name="android:textSize">20sp</item>

        <item name="colorControlActivated">@color/text_input_layout_color</item>
        <item name="colorControlHighlight">@color/text_input_layout_color</item>
    </style>

</resources>
